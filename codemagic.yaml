workflows:
  ios-workflow:
    name: iOS Workflow

    environment:
      flutter: stable

    scripts:
      - flutter pub get
      - flutter build ios --release --no-codesign

    artifacts:
      - build/ios/**/*.app